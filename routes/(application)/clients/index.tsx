import { Head } from "fresh/runtime";
import { define } from "~/utils.ts";

export default define.page(function Clients(_ctx) {
  return (
    <div class="p-4">
      <Head>
        <title>Clients - Deno demo project</title>
      </Head>
      <h1>Clients</h1>
    </div>
  );
});
